// Utility per autenticazione semplificata
// Questo file non usa next/headers per evitare problemi di build

export function isAuthenticated () {
  // Per ora restituisce sempre true per permettere il build
  // L'autenticazione reale verrà gestita dalle Server Actions
  return true
}

